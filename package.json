{
  "name": "database-az-service",
  "version": "0.1.0",
  "description": "Service for managing database interactions on Azure",
  "repository": {
    "type": "git",
    "url": "git@github.com:tdslalom/database-az-service.git"
  },
  "scripts": {
    "install:api": "cd api && npm install && cd ..",
    "install:tests": "cd tests/api && npm install && cd ..",
    "setup": "npm install && npm-run-all -l -p -r install:*"
  },
  "dependencies": {
    "npm-run-all": "^3.1.2",
    "tslint": "^4.5.1",
    "typescript": "^2.2.1"
  },
  "devDependencies": {},
  "author": "",
  "license": "ISC"
}
